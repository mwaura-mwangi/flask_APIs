[project]
name = "to-do-list"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask-sqlalchemy>=3.1.1",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.3",
]

[project.scripts]
to-do-list = "to_do_list:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
